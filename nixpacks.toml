# Fallback for Nixpacks builder (Railpack is primary; see railpack.json).
# Pin PHP 8.4 and Laravel public root if Nixpacks is used.
[variables]
NIXPACKS_PHP_VERSION = "8.4"
NIXPACKS_PHP_ROOT_DIR = "/app/public"
